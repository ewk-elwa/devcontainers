apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "helmchart.fullname" . }}-todo
  labels:
  {{- include "helmchart.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.todo.replicas }}
  selector:
    matchLabels:
      app: todo
    {{- include "helmchart.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        app: todo
      {{- include "helmchart.selectorLabels" . | nindent 8 }}
    spec:
      containers:
      - env:
        - name: DATABASE_URL
          value: {{ quote .Values.todo.todo.env.databaseUrl }}
        - name: KUBERNETES_CLUSTER_DOMAIN
          value: {{ quote .Values.kubernetesClusterDomain }}
        image: {{ .Values.todo.todo.image.repository }}:{{ .Values.todo.todo.image.tag
          | default .Chart.AppVersion }}
        name: todo
        ports:
        - containerPort: 8080
        resources: {}